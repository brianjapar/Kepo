<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kepo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kepo">
        <activity android:name=".SearchTodoActivity"></activity>
        <activity android:name=".DetailUserScreenActivity" />
        <activity android:name=".SearchUserActivity" />
        <activity android:name=".UpdateTodoActivity" />
        <activity android:name=".DetailTodoActivity" />
        <activity android:name=".GetTodoActivity" />
        <activity android:name=".CreateTodolistActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>